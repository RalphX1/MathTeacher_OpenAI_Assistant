# ===========================================
# MathTeacher OpenAI Assistant Configuration
# ===========================================

# Required: Your OpenAI API key (get from https://platform.openai.com/api-keys)
OPENAI_API_KEY=sk-your-api-key-here

# Optional: Reuse an existing assistant ID to avoid creating new ones
# Run the app once, note the assistant ID from console, then set it here
OPENAI_ASSISTANT_ID=

# Optional: OpenAI model (default: gpt-4-turbo)
OPENAI_MODEL=gpt-4-turbo

# Optional: Custom assistant configuration
ASSISTANT_NAME=Math Tutor
ASSISTANT_INSTRUCTIONS=You are a personal math tutor. Write and run code to answer math questions.

# Optional: Polling configuration (milliseconds)
POLLING_INTERVAL_MS=1000
POLLING_TIMEOUT_MS=120000
MAX_POLLING_RETRIES=60

# Server configuration
PORT=3000

# Rate limiting (requests per window)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=20

# CORS configuration (comma-separated origins, or * for all)
CORS_ALLOWED_ORIGINS=http://localhost:3000
